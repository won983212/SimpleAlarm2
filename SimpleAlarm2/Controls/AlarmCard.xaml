<UserControl x:Class="SimpleAlarm2.Controls.AlarmCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleAlarm2.Controls"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Height="80" Width="160">
    <Border x:Name="borderBackground" Background="{StaticResource Background}" BorderBrush="{StaticResource Gray300}"
            CornerRadius="5" BorderThickness="1">
        <Grid>
            <md:PackIcon Kind="Add" Width="32" Height="32" Foreground="{StaticResource Gray300}" 
                         HorizontalAlignment="Center" VerticalAlignment="Center" />
            <Grid x:Name="pnlAlarmContent" Visibility="Hidden">
                <StackPanel Margin="20,14">
                    <TextBlock x:Name="tblAlarmName" FontSize="9pt" Foreground="{StaticResource Gray700}">My alarm1</TextBlock>
                    <TextBlock x:Name="tblAlarmTime" FontSize="24pt" Margin="0,4,0,0">17:12</TextBlock>
                </StackPanel>
                <md:PackIcon Kind="Close" Margin="8" HorizontalAlignment="Right" VerticalAlignment="Top"
                         Foreground="{StaticResource Gray600}" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
